CREATE TABLE logs
(
    id            BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    payment_id    BIGINT,
    group_id      BIGINT,
    parent_log_id BIGINT,
    note          CLOB,
    CONSTRAINT pk_logs PRIMARY KEY (id)
);

ALTER TABLE logs
    ADD CONSTRAINT FK_LOGS_ON_GROUP FOREIGN KEY (group_id) REFERENCES groups (id);

ALTER TABLE logs
    ADD CONSTRAINT FK_LOGS_ON_PARENT_LOG FOREIGN KEY (parent_log_id) REFERENCES logs (id);

ALTER TABLE logs
    ADD CONSTRAINT FK_LOGS_ON_PAYMENT FOREIGN KEY (payment_id) REFERENCES payments (id);

ALTER TABLE groups
    ADD group_event_type VARCHAR(255);

ALTER TABLE payments
    ADD payment_event_type VARCHAR(255) NOT NULL;